// , Filter, Container

import styled from 'styled-components';

import { Box, Text } from 'components/ui';

export const TitleLabel = styled(Text).attrs({ fontSize: 1, color: 'grey' })``;

export const Header = styled(Box).attrs({ bg: 'grey1', px: 4, py: 2 })`
  width: 100%;
`;
